const locators = require('../../../utils/locators/callAI/locator');

// Example function using the imported locators
async function anotherAction(page) {
    // Action implementation
}

module.exports = {
    anotherAction,
};